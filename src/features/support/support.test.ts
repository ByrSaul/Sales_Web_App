import{describe,expect,it,vi}from'vitest';import type{ApiClient}from'../../core/api/apiClient';import{supportService,validateSupport}from'./supportService';
describe('support contract',()=>{it('sends exact Mobile identity, message and pure Base64 images',async()=>{const post=vi.fn().mockResolvedValue({Success:true,DebugMessage:'ok'});const r=await supportService({post}as unknown as ApiClient).send('user@x.com','  mensaje válido  ',[new File(['img'],'foto.png',{type:'image/png'})]);expect(post).toHaveBeenCalledWith('/support/send_email',{to:'soporte@foragro.com',cc:'user@x.com',from_email:'dynamics@foragro.com',subject:'Sales4App',body:'mensaje válido',attachments:[{file_name:'foto.png',file_content:'aW1n'}]},{timeoutMs:60000});expect(r).toMatchObject({success:true,message:'ok'})});it('omits attachments when empty',async()=>{const post=vi.fn().mockResolvedValue({success:true});await supportService({post}as unknown as ApiClient).send('u','0123456789',[]);expect(post.mock.calls[0][1]).not.toHaveProperty('attachments')});it('requires Mobile minimum message length',()=>expect(validateSupport(' corto ',[])).toMatch(/10/));it('allows images only',()=>expect(validateSupport('mensaje válido',[new File(['x'],'a.pdf',{type:'application/pdf'})])).toMatch(/imágenes/));it('enforces backend 5 MB per attachment',()=>expect(validateSupport('mensaje válido',[new File([new Uint8Array(5*1024*1024+1)],'a.png',{type:'image/png'})])).toMatch(/5 MB/))});
